import { Helmet } from "react-helmet";

const Main = () => {
    return (
        <div>
            <Helmet>
                <title>Main | Portfolio</title>
            </Helmet>
        </div>
    );
};

export default Main;
